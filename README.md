# 🛸 ORION: Drone Mission Control System



**ORION** — це система симуляції та керування автономними місіями дронів. Проект реалізовано на Python з використанням сучасної мікросервісної архітектури та **патернів проектування** (GoF) для забезпечення гнучкості, розширюваності та чистоти коду.



Система дозволяє запускати дрони різних типів (військові, рятувальні, аграрні) у різних середовищах (повітря, море, суша) та реагувати на погодні умови в реальному часі.



---



## 🛠 Технологічний стек



* **Мова:** Python 3.11+

* **Backend:** FastAPI

* **Frontend:** Streamlit

* **Архітектура:** OOP, Design Patterns



---



## 🏛 Архітектура та Патерни проектування



Цей проект є демонстрацією практичного застосування патернів проектування:



1.  **Factory Method (Фабричний метод):**

    * Використовується в `MissionFactory` для створення складних об'єктів місії (дрон, середовище, контролер) на основі конфігурації.

2.  **Bridge (Міст):**

    * Розділяє абстракцію керування (`DroneController`) від реалізації платформи (`AirPlatform`, `SeaPlatform`, `SurfacePlatform`). Це дозволяє керувати різними типами шасі через єдиний інтерфейс.

3.  **Strategy (Стратегія):**

    * Визначає поведінку дрона при зміні умов середовища (наприклад, `WindReactionStrategy` для повітря або `WaveReactionStrategy` для моря).

4.  **Template Method (Шаблонний метод):**

    * Клас `Drone` (base) визначає скелет виконання місії (`execute_mission`), а конкретні підкласи реалізують специфічні кроки (наприклад, `perform_payload_action`).

5.  **Chain of Responsibility (Ланцюжок обов'язків):**

    * Обробка критичних ситуацій (коригування висоти -> зміна маршруту -> екстрена посадка).

6.  **Observer (Спостерігач):**

    * Дрони "підписуються" на зміни у середовищі (вітер, температура) та реагують на події.



---



## 🚀 Функціонал



* **Підтримка 3-х середовищ:** Повітря, Море, Наземна поверхня.

* **Типи дронів:** Military, Rescue, Agriculture, Exploration, Pollution Monitoring.

* **Симуляція фізики:** Вплив вітру, хвиль, видимості на хід місії.

* **Інтерактивний UI:** Веб-панель керування та телеметрії (Logs, Status).



---



## ⚙️ Як запустити проект





```bash

#1. Клонування та встановлення залежностей

pip install -r requirements.txt

#2. Запуск Backend сервера(Сервер запуститься на http://localhost:8000)

python main.py

#3. Запуск Frontend інтерфейсу(Інтерфейс відкриється автоматично у браузері)

python -m streamlit run frontend.py   

```

